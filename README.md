# 중고거래 웹 플랫폼

Spring Boot + React 기반 중고거래 풀스택 웹 애플리케이션.

## 구조
- `backend/`  — Spring Boot REST API (JPA, MySQL)
- `frontend/` — React SPA (Vite)

## 실행
### backend
    cd backend && ./mvnw spring-boot:run
### frontend
    cd frontend && npm install && npm run dev
